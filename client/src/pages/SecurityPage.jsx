import { useState } from "react";export default function SecurityPage() {    const [state, setState] = useState({        twoFactor: false,        loginAlerts: true,        sessionTimeout: true,        rememberDevice: false,    });    const toggle = (key) =>        setState((prev) => ({            ...prev,            [key]: !prev[key],        }));    return (        <section className="container section">            <h2>Security</h2>            <article className="glass-card form-grid">                <label className="switch-row">                    <span>Two-Factor Authentication</span>                    <input type="checkbox" checked={state.twoFactor} onChange={() => toggle("twoFactor")} />                </label>                <label className="switch-row">                    <span>Login Alerts</span>                    <input                        type="checkbox"                        checked={state.loginAlerts}                        onChange={() => toggle("loginAlerts")}                    />                </label>                <label className="switch-row">                    <span>Session Timeout</span>                    <input                        type="checkbox"                        checked={state.sessionTimeout}                        onChange={() => toggle("sessionTimeout")}                    />                </label>                <label className="switch-row">                    <span>Remember Device</span>                    <input                        type="checkbox"                        checked={state.rememberDevice}                        onChange={() => toggle("rememberDevice")}                    />                </label>            </article>        </section>    );}