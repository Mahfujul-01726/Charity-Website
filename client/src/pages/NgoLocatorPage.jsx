import { useState, useMemo, useEffect } from "react";import { useNavigate } from "react-router-dom";import { ngoList } from "../data/mockData";import { MapContainer, TileLayer, Marker, Popup, useMap } from "react-leaflet";import "leaflet/dist/leaflet.css";import L from "leaflet";delete L.Icon.Default.prototype._getIconUrl;L.Icon.Default.mergeOptions({    iconRetinaUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png",    iconUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png",    shadowUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png",});function MapController({ center, zoom }) {    const map = useMap();    useEffect(() => {        map.flyTo(center, zoom, { animate: true, duration: 1.5 });    }, [center, zoom, map]);    return null;}export default function NgoLocatorPage() {    const navigate = useNavigate();    const defaultCenter = [34.0522, -118.2437]; // LA    const [center, setCenter] = useState(defaultCenter);    const [searchQuery, setSearchQuery] = useState("");    const [loadingLoc, setLoadingLoc] = useState(false);    const [zoom, setZoom] = useState(13);    const [selectedNgo, setSelectedNgo] = useState(null);    const filteredNgos = useMemo(() => {        if (!searchQuery) return ngoList;        const q = searchQuery.toLowerCase();        return ngoList.filter(n => n.name.toLowerCase().includes(q) || n.category.toLowerCase().includes(q));    }, [searchQuery]);    const handleLocateMe = () => {        if (!navigator.geolocation) {            alert("Geolocation is not supported by your browser");            return;        }        setLoadingLoc(true);        navigator.geolocation.getCurrentPosition(            (pos) => {                setCenter([pos.coords.latitude, pos.coords.longitude]);                setZoom(14);                setLoadingLoc(false);            },            () => {                alert("Unable to retrieve your location");                setLoadingLoc(false);            }        );    };    const handleSelectNgo = (ngo) => {        setCenter([ngo.lat, ngo.lng]);        setZoom(16);        setSelectedNgo(ngo);    };    return (        <section className="container section">            <div style={{ textAlign: 'center', marginBottom: '40px' }}>                <h2><i className="ph ph-map-pin-line"></i> Global NGO Locator</h2>                <p style={{ color: 'var(--text-secondary)' }}>Find and connect with verified charitable organizations in your area.</p>            </div>            <div className="two-col" style={{ alignItems: 'start' }}>                <article className="glass-card" style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>                    <div style={{ display: 'flex', gap: '12px' }}>                        <div style={{ position: 'relative', flex: 1 }}>                            <i className="ph ph-magnifying-glass" style={{ position: 'absolute', left: '16px', top: '50%', transform: 'translateY(-50%)', color: 'var(--text-secondary)' }}></i>                            <input                                 placeholder="Search NGOs by name or cause..."                                 value={searchQuery}                                onChange={(e) => setSearchQuery(e.target.value)}                                style={{ paddingLeft: '40px', margin: 0 }}                            />                        </div>                        <button className="btn btn-secondary" onClick={handleLocateMe} disabled={loadingLoc} title="Find My Location">                            {loadingLoc ? <i className="ph ph-spinner-gap ph-spin"></i> : <i className="ph ph-crosshair"></i>}                        </button>                    </div>                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>                        <h3 style={{ margin: 0 }}><i className="ph ph-buildings"></i> Nearby NGOs</h3>                        <span className="chip">{filteredNgos.length} Found</span>                    </div>                    <ul className="list-clean" style={{ maxHeight: '500px', overflowY: 'auto', paddingRight: '8px' }}>                        {filteredNgos.map((ngo) => (                            <li                                 className="list-item"                                 key={ngo.name}                                 onClick={() => handleSelectNgo(ngo)}                                style={{                                     padding: '16px',                                     background: selectedNgo?.name === ngo.name ? 'rgba(6, 182, 212, 0.15)' : 'rgba(255,255,255,0.02)',                                     borderRadius: '16px',                                     marginBottom: '12px',                                     border: `1px solid ${selectedNgo?.name === ngo.name ? 'var(--cyan)' : 'var(--glass-border)'}`,                                    cursor: 'pointer',                                    transition: 'all 0.3s ease',                                    display: 'block'                                }}                            >                                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: '8px' }}>                                    <strong style={{ fontSize: '1.1rem' }}>{ngo.name}</strong>                                    <span style={{ background: 'rgba(245, 158, 11, 0.2)', color: 'var(--warning)', padding: '4px 8px', borderRadius: '8px', fontSize: '0.85rem', fontWeight: 'bold' }}>                                        <i className="ph-fill ph-star"></i> {ngo.rating}                                    </span>                                </div>                                <div style={{ display: 'flex', flexWrap: 'wrap', gap: '8px', marginBottom: '8px' }}>                                    <span style={{ color: 'var(--cyan)', fontSize: '0.9rem', display: 'flex', alignItems: 'center', gap: '4px' }}>                                        <i className="ph ph-tag"></i> {ngo.category}                                    </span>                                    <span style={{ color: 'var(--text-secondary)', fontSize: '0.9rem', display: 'flex', alignItems: 'center', gap: '4px' }}>                                        <i className="ph ph-map-pin"></i> {ngo.distance} away                                    </span>                                </div>                            </li>                        ))}                        {filteredNgos.length === 0 && (                            <div style={{ textAlign: 'center', padding: '40px 0', color: 'var(--text-secondary)' }}>                                <i className="ph ph-magnifying-glass-minus" style={{ fontSize: '3rem', marginBottom: '16px', display: 'block' }}></i>                                <p>No NGOs match your search.</p>                            </div>                        )}                    </ul>                </article>                <article className="glass-card map-placeholder" style={{ padding: 0, overflow: 'hidden', height: '650px', position: 'sticky', top: '100px' }}>                    <MapContainer center={center} zoom={zoom} style={{ height: "100%", width: "100%" }}>                        <TileLayer                            attribution='&copy; <a href="https://osm.org/copyright">OpenStreetMap</a> contributors'                            url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"                        />                        <MapController center={center} zoom={zoom} />                        {filteredNgos.map((ngo) => (                            <Marker key={ngo.name} position={[ngo.lat, ngo.lng]}>                                <Popup>                                    <div style={{ padding: '4px', textAlign: 'center' }}>                                        <strong style={{ display: 'block', marginBottom: '4px', fontSize: '1rem', color: '#111827' }}>{ngo.name}</strong>                                        <span style={{ display: 'block', marginBottom: '8px', color: '#6b7280' }}>{ngo.category}</span>                                        <button                                             className="btn"                                             style={{ padding: '6px 12px', fontSize: '0.85rem', width: '100%', display: 'inline-block' }}                                            onClick={() => navigate(`/contact?subject=Contacting ${encodeURIComponent(ngo.name)}`)}                                        >                                            Contact NGO                                        </button>                                    </div>                                </Popup>                            </Marker>                        ))}                    </MapContainer>                </article>            </div>        </section>    );}